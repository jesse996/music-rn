export interface IInfo {
	cover_img_url: string;
	source_url: string;
	id: string;
	title: string;
}

export interface ITrack {
	album: string;
	album_id: string;
	artist: string;
	artist_id: string;
	id: string;
	img_url: string;
	source: string;
	source_url: string;
	title: string;
}
export interface ISong {
	id: string;
	title: string;
	img: string;
}
